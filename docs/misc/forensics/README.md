# DFIR
## NetworkMiner
```sh
sudo apt update
sudo apt install -y mono-complete unzip
wget https://www.netresec.com/?download=NetowrkMiner> -O NetworkMiner.zip
uznip NetworkMiner.zip -d NetworkMiner
cd NetworkMiner
sudo mono NetworkMiner.exe
sudo mono NetworkMineer.exe -r /path/to/your/file.pcap
```

